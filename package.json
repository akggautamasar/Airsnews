{
  "name": "telegram-news-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Telegram bot to send news from AirShorts API to a channel.",
  "main": "api/send-news.js",
  "scripts": {
    "start": "node api/send-news.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-telegram-bot-api": "^0.61.0",
    "node-fetch": "^3.3.0"
  },
  "devDependencies": {},
  "engines": {
    "node": "20.x"
  }
}
